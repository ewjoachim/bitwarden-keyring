[project]
name = "bitwarden-keyring"
version = "2.0.0"
description = "A Bitwarden backend for the keyring Python library"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["keyring>=25.6.0"]
